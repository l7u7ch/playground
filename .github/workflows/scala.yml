on: [push]

jobs:
  example-job:
    runs-on: ubuntu-latest

    steps:
      - name: X100
        uses: actions/checkout@v4

      - name: X101
        uses: actions/setup-java@v3
        with:
          java-version: "11"
          distribution: "temurin"
          cache: "sbt"

      - name: X102
        run: sbt test
