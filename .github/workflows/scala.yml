on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: XXX
        uses: actions/checkout@v4

      - uses: actions/setup-java@v3
        with:
          java-version: "11"
          distribution: "temurin"
          cache: "sbt"

      - run: sbt test
